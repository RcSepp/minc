import <builtin.acc>;

stmtdef testExprASTMembers $L $I $B {
	printf("$0.value == %s\n\n", $0.value);

	printf("$1.name == %s\n\n", $1.name);

	printf("parentBlock == 0x%lx\n", parentBlock);
	printf("$2.parent == 0x%lx\n", $2.parent);
	//codegen($2, parentBlock);
	$2.parent = parentBlock;
	printf("$2.parent == 0x%lx\n", $2.parent);
}

testExprASTMembers 123 ident {}
